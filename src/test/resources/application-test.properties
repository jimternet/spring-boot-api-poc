# Spring Data Cassandra Configuration
spring.data.cassandra.keyspace-name=inventory_test
spring.data.cassandra.schema-action=CREATE_IF_NOT_EXISTS
spring.data.cassandra.local-datacenter=datacenter1
# Contact points and port will be set by TestCassandraConfig.Initializer

# Legacy Cassandra Configuration
cassandra_keyspace=inventory_test
# Contact points and port will be set by TestCassandraConfig.Initializer
cassandra_local-datacenter=datacenter1

# Hazelcast Configuration
# URL and members will be set by TestHazelcastConfig.Initializer
hz_username=admin
hz_password=admin

# Disable Hazelcast Management Center for tests
spring.hazelcast.config=classpath:hazelcast-test.xml 